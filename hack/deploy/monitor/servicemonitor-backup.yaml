apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: stash-servicemonitor
  namespace: ${STASH_NAMESPACE}
  labels:
    app: stash
spec:
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: stash
  endpoints:
  - port: pushgateway
