apiVersion: stash.appscode.com/v1alpha1
kind: Restic
metadata:
  name: stash-restic
  namespace: default
spec:
  selector:
    matchLabels:
      app: stash-restic
  fileGroups:
  - path: /source/data
    retentionPolicy:
      keepLast: 5
      prune: true
  backend:
    stash:
      container: stashqa
      prefix: demo
    storageSecretName: stash-secret
  schedule: '@every 1m'
  volumeMounts:
  - mountPath: /source/data
    name: source-data
